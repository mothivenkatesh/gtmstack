[project]
name = "app"
version = "0.1.0"
dependencies = [ "flask>=3.0", "flask-cors>=4.0", "youtube-transcript-api>=1.2", "requests>=2.31", "curl-cffi>=0.7", "anthropic>=0.40", "psycopg[binary]>=3.1", "gspread>=6.0", "opentelemetry-api>=1.27", "opentelemetry-sdk>=1.27", "opentelemetry-exporter-otlp-proto-http>=1.27" ]
requires-python = "~=3.12.0"
